awk -F= '
BEGIN { OFS="=" }
NF >= 1 {
  key=$1
  if (key ~ /PASSWORD|SECRET|TOKEN|KEY/) {
    print key, "CAMBIAR_EN_DESPLIEGUE"
  } else {
    print $0
  }
}
' /opt/securetech/.env > /opt/securetech/.env.example
